import 'package:flutter/material.dart';

/// A class that defines the color scheme used in the application.
class AppColors {
  /// The primary color used throughout the application.
  static const Color rootPrimaryColor = Color(0xFF6F35A5);

  /// The light version of the primary color used throughout the application.
  static const Color rootPrimaryLightColor = Color(0xFFF1E6FF);
}

/// A class that defines the sizes used in the application.
class AppSizes {
  /// The default padding size used throughout the application.
  static const double defaultPadding = 16.0;
}